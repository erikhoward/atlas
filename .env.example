# Atlas Environment Variables Example
# Copy this file to .env and fill in your actual values
#
# IMPORTANT: The .env file is automatically loaded by Atlas when it starts.
# This works for both:
#   - Running Atlas directly: cargo run -- export
#   - Running with Docker Compose: docker-compose up
#
# NOTE: Only set the environment variables for the database you're using.
# If using PostgreSQL, you don't need ATLAS_COSMOSDB_KEY.
# If using Cosmos DB, you don't need ATLAS_PG_PASSWORD.

# ============================================================================
# OpenEHR Server Credentials (REQUIRED)
# ============================================================================
ATLAS_OPENEHR_USERNAME=your_openehr_username
ATLAS_OPENEHR_PASSWORD=your_openehr_password

# ============================================================================
# PostgreSQL Credentials (Required if database_target = "postgresql")
# ============================================================================
ATLAS_PG_PASSWORD=your_postgres_password

# ============================================================================
# Azure Cosmos DB Credentials (Required if database_target = "cosmosdb")
# ============================================================================
# ATLAS_COSMOSDB_KEY=your_cosmosdb_primary_key

# ============================================================================
# Logging Configuration (Optional)
# ============================================================================
# Log level: trace, debug, info, warn, error
RUST_LOG=info

# ============================================================================
# Azure Application Insights (Optional)
# ============================================================================
# Uncomment and fill in if using Azure Log Analytics integration
# ATLAS_AZURE_TENANT_ID=your_azure_tenant_id
# ATLAS_AZURE_CLIENT_ID=your_azure_client_id
# ATLAS_AZURE_CLIENT_SECRET=your_azure_client_secret

